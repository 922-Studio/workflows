# Warp Notes

## Update GitHub Actions Documentation

- Workflows live in `.github/workflows` in this repo.
- Home lab documentation for GitHub Actions lives in `../HomeStructure/docs/services/github-runner.md` under **GitHub Actions Workflows**.
- When you add, rename, or remove workflows here, update that section so the list stays in sync.
- Keep each workflow entry to a single short sentence plus a minimal `uses:` example.
- Follow the documentation rules in `../HomeStructure/.ai/documentation-guidelines.md` (commands first, minimal text) when editing the HomeStructure docs.
