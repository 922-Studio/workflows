name: PR Preview Environment

# Reusable workflow — manages Docker-based PR preview environments.
#
# Spin up a live preview container stack when a PR is opened/updated,
# tear it down when the PR is closed.
#
# How it works:
#   1. On PR open/sync  → deploy-preview job runs pr-demo.sh start
#   2. On PR close      → cleanup-preview job runs pr-demo.sh stop
#   3. A PR comment is created/updated with the preview URL after each event.
#
# Callers must pass at minimum: repository_path, project_name, PAT_GITHUB.
#
# Example caller workflow:
#   jobs:
#     pr-preview:
#       uses: 922-Studio/workflows/.github/workflows/pr-demo.yml@main
#       with:
#         repository_path: /home/lab/Drafter
#         project_name: drafter
#       secrets:
#         PAT_GITHUB: ${{ secrets.PAT_GITHUB }}

on:
  workflow_call:
    inputs:
      repository_path:
        required: true
        type: string
        description: "Absolute path to the repository on the self-hosted runner, e.g. /home/lab/Drafter"
      project_name:
        required: true
        type: string
        description: "Short identifier for the project, e.g. drafter. Used in container names and state files."
      port_base:
        required: false
        type: string
        default: "9100"
        description: "Base port for preview environments. Preview port = port_base + (PR number % port_range)."
      port_range:
        required: false
        type: string
        default: "100"
        description: "Port range before wrapping. PR 100 reuses port of PR 0, PR 200 reuses PR 0, etc."
      compose_file:
        required: false
        type: string
        default: "docker-compose.yaml"
        description: "Docker Compose file to use, relative to the repository root."
      healthcheck_path:
        required: false
        type: string
        default: "/api/health"
        description: "HTTP path used to verify the preview container is up and healthy."
      healthcheck_timeout:
        required: false
        type: string
        default: "60"
        description: "Seconds to wait for the healthcheck to pass before reporting the preview as started."
      max_demos:
        required: false
        type: string
        default: "5"
        description: "Maximum number of concurrent previews allowed on the runner."
      preview_domain:
        required: false
        type: string
        default: ""
        description: "Domain for public preview URLs, e.g. '922-studio.com'. Generates {project}-pr-{N}.{domain}. When empty, falls back to Tailscale IP."
      tailscale_ip:
        required: false
        type: string
        default: "100.112.171.16"
        description: "Tailscale IP of the self-hosted runner. Only used when preview_domain is empty."
      action:
        required: true
        type: string
        description: "PR action: 'start' (opened/synchronize) or 'stop' (closed). Caller must pass this since github.event.action is not reliable in reusable workflows."
    secrets:
      PAT_GITHUB:
        required: true
        description: "Personal Access Token for checking out the workflows repo and posting PR comments."

# ── Deploy Preview (PR opened or updated) ────────────────────────────────────

jobs:
  deploy-preview:
    name: Deploy PR Preview
    runs-on: self-hosted
    if: inputs.action == 'start'

    steps:
      - name: Log preview deployment start
        run: |
          echo "=========================================="
          echo "PR Preview — Deploy"
          echo "=========================================="
          echo "Project:    ${{ inputs.project_name }}"
          echo "Repo path:  ${{ inputs.repository_path }}"
          echo "PR number:  ${{ github.event.pull_request.number }}"
          echo "Branch:     ${{ github.head_ref }}"
          echo "Port base:  ${{ inputs.port_base }}"
          echo "Compose:    ${{ inputs.compose_file }}"
          echo "Timestamp:  $(date '+%Y-%m-%d %H:%M:%S')"
          echo "=========================================="

      # Checkout the workflows repo to get the latest pr-demo.sh script.
      # We place it under workflow-scripts/ to keep the workspace clean.
      - name: Checkout workflow scripts
        uses: actions/checkout@v5
        with:
          repository: 922-Studio/workflows
          ref: main
          path: workflow-scripts
          token: ${{ secrets.PAT_GITHUB }}

      - name: Make pr-demo.sh executable
        run: chmod +x workflow-scripts/.github/scripts/pr-demo.sh

      # Run the start command with all configuration via env vars.
      # Output is captured to extract the preview URL for the PR comment.
      # We use set +e to ensure output is always displayed, even on failure.
      - name: Start PR preview
        id: start_preview
        env:
          PROJECT_NAME: ${{ inputs.project_name }}
          REPO_PATH: ${{ inputs.repository_path }}
          PORT_BASE: ${{ inputs.port_base }}
          PORT_RANGE: ${{ inputs.port_range }}
          MAX_DEMOS: ${{ inputs.max_demos }}
          COMPOSE_FILE: ${{ inputs.compose_file }}
          PREVIEW_DOMAIN: ${{ inputs.preview_domain }}
          TAILSCALE_IP: ${{ inputs.tailscale_ip }}
          HEALTHCHECK_PATH: ${{ inputs.healthcheck_path }}
          HEALTHCHECK_TIMEOUT: ${{ inputs.healthcheck_timeout }}
        run: |
          echo "Starting preview for PR #${{ github.event.pull_request.number }}..."

          set +e
          OUTPUT=$(workflow-scripts/.github/scripts/pr-demo.sh \
            start \
            "${{ github.event.pull_request.number }}" \
            "${{ github.head_ref }}" 2>&1)
          EXIT_CODE=$?
          set -e

          echo "$OUTPUT"

          if [[ $EXIT_CODE -ne 0 ]]; then
            echo "::error::pr-demo.sh exited with code $EXIT_CODE"
            exit $EXIT_CODE
          fi

          # Extract the URL from STARTED output line, e.g.:
          #   STARTED pr=42 url=https://drafter-pr-42.922-studio.com branch=feature/xyz
          PREVIEW_URL=$(echo "$OUTPUT" | grep '^STARTED' | grep -o 'url=[^ ]*' | cut -d= -f2)

          if [[ -z "$PREVIEW_URL" ]]; then
            echo "WARNING: Could not extract preview URL from output"
            if [[ -n "${{ inputs.preview_domain }}" ]]; then
              PREVIEW_URL="https://${{ inputs.project_name }}-pr-${{ github.event.pull_request.number }}.${{ inputs.preview_domain }}"
            else
              PREVIEW_URL="http://${{ inputs.tailscale_ip }}:$(( ${{ inputs.port_base }} + (${{ github.event.pull_request.number }} % ${{ inputs.port_range }}) ))"
            fi
          fi

          echo "preview_url=${PREVIEW_URL}" >> "$GITHUB_OUTPUT"
          echo "Preview URL: ${PREVIEW_URL}"

      # Post a new comment on the PR with the preview URL.
      # Uses GITHUB_TOKEN so comments appear from github-actions[bot].
      - name: Post preview URL comment
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S UTC')
          PREVIEW_URL="${{ steps.start_preview.outputs.preview_url }}"
          BRANCH="${{ github.head_ref }}"
          COMMIT="${{ github.event.pull_request.head.sha }}"
          SHORT_COMMIT="${COMMIT:0:7}"

          COMMENT_BODY=$(cat <<COMMENT_EOF
          ### PR Preview

          | | |
          |---|---|
          | **Status** | Live |
          | **URL** | ${PREVIEW_URL} |
          | **Branch** | \`${BRANCH}\` |
          | **Commit** | \`${SHORT_COMMIT}\` |
          | **Updated** | ${TIMESTAMP} |
          COMMENT_EOF
          )
          COMMENT_BODY=$(echo "$COMMENT_BODY" | sed 's/^          //')

          gh pr comment "${{ github.event.pull_request.number }}" \
            --repo "${{ github.repository }}" \
            --body "$COMMENT_BODY"

          echo "PR comment posted"

      - name: Show logs on failure
        if: failure()
        env:
          PROJECT_NAME: ${{ inputs.project_name }}
        run: |
          PROJ="${PROJECT_NAME}-pr-${{ github.event.pull_request.number }}"
          WORKTREE="/tmp/${PROJECT_NAME}-pr/pr-${{ github.event.pull_request.number }}"

          echo "=========================================="
          echo "DEPLOY FAILED — Diagnostics"
          echo "=========================================="

          echo ""
          echo "── Container status ──"
          docker compose -p "$PROJ" ps -a 2>/dev/null || echo "(no compose project found)"

          echo ""
          echo "── Container logs ──"
          docker compose -p "$PROJ" logs --tail=150 2>/dev/null || echo "(no logs available)"

          echo ""
          echo "── Container inspect (last exit) ──"
          for cid in $(docker compose -p "$PROJ" ps -aq 2>/dev/null); do
            echo "--- Container: $cid ---"
            docker inspect "$cid" --format '{{.State.Status}} exit={{.State.ExitCode}} error={{.State.Error}}' 2>/dev/null || true
          done

          echo ""
          echo "── Worktree .env (redacted) ──"
          if [[ -f "${WORKTREE}/.env" ]]; then
            grep -v -iE '(password|secret|token|key|auth)' "${WORKTREE}/.env" 2>/dev/null || echo "(could not read)"
          else
            echo "(no .env found at ${WORKTREE})"
          fi

          echo ""
          echo "── Docker system status ──"
          docker system df || true

      - name: Log deploy result
        if: always()
        run: |
          echo "=========================================="
          echo "PR Preview Deploy — ${{ job.status == 'success' && 'SUCCESS' || 'FAILED' }}"
          echo "=========================================="
          echo "PR:        #${{ github.event.pull_request.number }}"
          echo "Branch:    ${{ github.head_ref }}"
          echo "URL:       ${{ steps.start_preview.outputs.preview_url }}"
          echo "Timestamp: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "=========================================="

# ── Cleanup Preview (PR closed) ───────────────────────────────────────────────

  cleanup-preview:
    name: Cleanup PR Preview
    runs-on: self-hosted
    if: inputs.action == 'stop'

    steps:
      - name: Log preview cleanup start
        run: |
          echo "=========================================="
          echo "PR Preview — Cleanup"
          echo "=========================================="
          echo "Project:   ${{ inputs.project_name }}"
          echo "PR number: ${{ github.event.pull_request.number }}"
          echo "Reason:    PR ${{ github.event.pull_request.merged && 'merged' || 'closed' }}"
          echo "Timestamp: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "=========================================="

      - name: Checkout workflow scripts
        uses: actions/checkout@v5
        with:
          repository: 922-Studio/workflows
          ref: main
          path: workflow-scripts
          token: ${{ secrets.PAT_GITHUB }}

      - name: Make pr-demo.sh executable
        run: chmod +x workflow-scripts/.github/scripts/pr-demo.sh

      - name: Stop PR preview
        env:
          PROJECT_NAME: ${{ inputs.project_name }}
          REPO_PATH: ${{ inputs.repository_path }}
          PORT_BASE: ${{ inputs.port_base }}
          PORT_RANGE: ${{ inputs.port_range }}
          MAX_DEMOS: ${{ inputs.max_demos }}
          COMPOSE_FILE: ${{ inputs.compose_file }}
          PREVIEW_DOMAIN: ${{ inputs.preview_domain }}
          TAILSCALE_IP: ${{ inputs.tailscale_ip }}
          HEALTHCHECK_PATH: ${{ inputs.healthcheck_path }}
          HEALTHCHECK_TIMEOUT: ${{ inputs.healthcheck_timeout }}
        run: |
          echo "Stopping preview for PR #${{ github.event.pull_request.number }}..."

          workflow-scripts/.github/scripts/pr-demo.sh \
            stop \
            "${{ github.event.pull_request.number }}"

      # Post a new comment to show the preview has been taken down.
      - name: Post teardown comment
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S UTC')
          REASON="${{ github.event.pull_request.merged && 'merged' || 'closed' }}"
          BRANCH="${{ github.head_ref }}"

          COMMENT_BODY=$(cat <<COMMENT_EOF
          ### PR Preview

          | | |
          |---|---|
          | **Status** | Offline (PR ${REASON}) |
          | **Branch** | \`${BRANCH}\` |
          | **Stopped** | ${TIMESTAMP} |

          > The preview environment has been stopped and removed.
          COMMENT_EOF
          )
          COMMENT_BODY=$(echo "$COMMENT_BODY" | sed 's/^          //')

          gh pr comment "${{ github.event.pull_request.number }}" \
            --repo "${{ github.repository }}" \
            --body "$COMMENT_BODY"

          echo "PR comment posted — preview offline"

      - name: Log cleanup result
        if: always()
        run: |
          echo "=========================================="
          echo "PR Preview Cleanup — ${{ job.status == 'success' && 'SUCCESS' || 'FAILED' }}"
          echo "=========================================="
          echo "PR:        #${{ github.event.pull_request.number }}"
          echo "Timestamp: $(date '+%Y-%m-%d %H:%M:%S')"
          echo "=========================================="
